<%= form_with(model: [ @product, @product.lineitems.build ]) do |form| %>
  <% if lineitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lineitem.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% lineitem.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
